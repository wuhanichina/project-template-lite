# project-template-lite Legacy Workspace Rule

This file exists for Antigravity installations or workflows that still load
workspace rules from `.agent/rules/`.

The current workspace rule is `.agents/rules/project-template-lite.md`.
`AGENTS.md` and `.cursor/rules/` remain the shared source of truth.

Before editing code, documentation, comments, generated prose, or template
structure, read `AGENTS.md` and every rule file it lists.
