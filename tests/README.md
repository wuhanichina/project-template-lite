# tests

Put temporary validation, tuning checks, smoke tests, and local debugging helpers
here.

Tests can support development, but a result should not be treated as paper
evidence until it is produced by a case entry or a documented validation entry
and registered in `01_IDEA/evidence_map.md`.
